# Unfortunately this doesn't install
# automatically because of the KEXT
# needs to be approved manually by the
# user
- name: Install VirtualBox
  homebrew_cask:
    name: virtualbox
    state: latest

- name: Install VirtualBox Extension Pack
  homebrew_cask:
    name: virtualbox-extension-pack
    state: latest
